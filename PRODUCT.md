# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- **주 사용자 — 스트릿댄스 동아리 팀원(주로 후배).** 정기 연습 시간 외에 자기 자세를
  점검할 방법이 없는 사람들. 연습 영상을 올려 기준(프로/레퍼런스) 영상과 비교해
  "어디가 어긋났는지" 피드백을 받는다.
- **부차 사용자 — 팀장(리더).** 팀원에게 숙제(안무 과제)를 부여하고, 팀원이 제출한
  연습 기록을 확인한다. `isLeader` 플래그로 구분되며 '숙제 생성' 기능을 추가로 가진다.
- **부차 관람자 — 포트폴리오 평가자(개별연구 심사·채용 담당).** 이 서비스는
  동국대 25-1 개별연구이자 포트폴리오의 산출물이기도 하다. 설득 우선순위는
  **실사용 댄서가 1순위**, 평가자는 완성도로 함께 설득한다(실사용을 해치지 않는 선에서).

## Product Purpose

프로/레퍼런스 댄서 영상과 사용자의 연습 영상을 비교해 자세가 어긋난 지점을
자동으로 짚어 주는 AI 댄스 분석 서비스. 팀장이 자리에 없어도 후배가 스스로 춤을
점검할 수 있게 하는 것이 존재 이유다. 성공은 "코치 없이 혼자서도 신뢰할 수 있는
동작 피드백을 받는" 것.

## Positioning

이웃 제품이 그대로 베낄 수 없는 것은 **비교의 엄밀함**이다. "두 영상을 비교한다"를
세 축으로 분해해 각각을 검증했다:

1. **탐지** — 관절이 제대로 잡히는가 (YOLO-pose 4계열 비교 후 선정)
2. **정렬** — 서로 다른 시간대의 같은 동작이 매핑되는가 (DTW `dtw_ndim` 다변량 정렬,
   psi로 끝점 강제 완화)
3. **표현** — 촬영 조건(카메라 거리·위치·기울기) 차이가 동작 오차로 오인되지 않는가
   (좌표 대신 관절 각도로 비교)

정답(GT)을 합성 변형본으로 dense하게 만들어 "정렬 기계가 의도대로 작동하는가"를
수치로 증명한 것이 핵심 차별점. 점수를 내는 것이 아니라 **그 점수가 맞다는 것을
증명한 것**이 이 제품의 위치다.

## Operating Context

- **비동기 + 폴링 흐름.** 분석에 1~2분이 걸려 요청을 붙잡을 수 없다. 사용자는
  업로드 → 구간 지정 → 분석 요청(202 즉시 반환) → 결과가 COMPLETED가 될 때까지
  대기/폴링 → 결과 확인의 흐름을 거친다. 대기 시간이 UX의 실제 일부다.
- **영상 촬영·업로드·결과 확인이 실제 사용 장면.** 연습실에서 폰으로 찍은 영상을
  올리고, 나중에 결과를 다시 열어 본다.
- **핵심 화면(현 구현):** 대시보드, 자유 연습(기준·연습 영상 업로드 + 구간 트림),
  숙제(목록·상세·생성), 내 기록(목록·상세). 결과 화면은 기준/연습 영상을 동기화해
  나란히 비교(SyncedComparison)한다.
- **로그인 필수.** 이메일 가입과 OAuth2(카카오·네이버). 팀 소속 여부에 따라
  숙제 기능 노출이 달라진다.

## Capabilities and Constraints

- **기능:** 영상 업로드(presigned PUT로 R2에 직접), 구간 트림(타임스탬프만 저장,
  재인코딩 없음), 포즈 추출·DTW 정렬·구간 피드백, 결과 이미지·비교 영상 생성,
  팀 숙제 부여·제출, 개인 연습 기록 조회.
- **결과물 URL은 저장하지 않고 조회 시마다 새 presigned URL을 발급한다.** DB에는
  객체 키만 둔다. → 화면은 "결과가 만료되어 다시 불러오는" 상태를 자연스럽게 다뤄야 한다.
- **분석은 비동기다.** COMPLETED/처리 중/실패 상태를 사용자에게 성실히 보여줘야 한다.
- **언어: 한국어.** 카피·라벨·에러 모두 한국어가 기본.
- **용어:** 자유 연습 / 숙제(assignment) / 내 기록(practice log) / 기준 영상 /
  연습 영상 / 팀 / 팀장(리더).
- **스택(기존 코드가 확정):** Frontend React 19 + Vite, API Spring Boot 3.4 / Java 17,
  AI FastAPI / Python 3.10, 저장소 Cloudflare R2, 인증 Spring Security + JWT + OAuth2.

## Brand Commitments

- **정식 이름은 `ODO`.** (배포 도메인 `odostudio.site`와 일치. README 제목의
  "Dance Analyzer", 현 헤더의 "DanceFlow"는 정리 대상 — 향후 작업은 ODO로 통일한다.)
- **AI 특유의 이모지는 절대 금지 (사용자가 명시한 강한 제약).** 💃 🎯 📋 📊 👑 👋
  같은 장식용/AI가 남발하는 이모지를 UI에 쓰지 않는다. 현 구현(Header 로고 💃,
  Dashboard 피처 카드 아이콘, 환영 문구 👋 등)은 이 원칙과 충돌하므로 향후 작업에서
  걷어낸다. 아이콘이 필요하면 이모지가 아닌 제대로 된 아이콘/타이포/도형으로 해결한다.

## Evidence on Hand

- **동작하는 풀스택 구현.** `web-service/frontend`(React), `web-service/api-server`(Spring),
  `ai-server`(FastAPI), `model-research`(방법론 검증 코드).
- **검증 산출물이 실재한다.** 합성 GT, DTW 개선 전/후 정렬 정확도 수치(예: offset
  변형에서 61%→100% 대응, 중앙 오차 2.000초→0.000초), pycocotools 평가 결과.
  README 3장에 상세.
- **초기 프로토타입을 정성 평가했다.** 간단한 기본기 동작의 결과를 영상과 나란히
  확인해 혼자 자세를 점검하는 보조 도구로서의 가능성을 확인했다. 현재 엔진의 사용자
  효과를 정량화한 결과와는 구분한다.
- **배포 진행 중** — `odostudio.site`, `deploy/`에 Nginx·systemd·launchd 설정.
- **기간:** 2025.03–현재(동국대학교 2025-1 개별연구 시작 기준).
- **기획 문서 PDF 2종** (`새싹_박준하_AI 댄스 분석 플랫폼.pdf` 등)이 루트에 존재.
- **아직 없는 것 (지어내지 말 것):** 데모 GIF·결과 스크린샷(README에 TODO로 비어 있음),
  현재 엔진의 정량 사용자 효과 지표.

## Product Principles

1. **엄밀함을 눈에 보이게.** 이 제품의 정체성은 "믿을 수 있는 비교"다. 점수·피드백은
   근거(어느 구간, 어느 관절)와 함께 제시하고, 신뢰를 흔드는 과장된 표현을 피한다.
2. **기다림도 경험이다.** 분석은 비동기다. 처리 중·완료·실패·만료 상태를 숨기지 말고
   성실하게, 불안하지 않게 보여준다.
3. **실사용 댄서가 1순위, 완성도로 평가자까지.** 연습실 현장에서 쓰기 편해야 하고,
   그 완성도가 포트폴리오로서도 설득력을 갖는다. 둘이 충돌하면 실사용을 택한다.
4. **모바일과 데스크톱 모두 1급.** 촬영·업로드는 모바일, 상세 비교는 데스크톱 —
   양쪽 모두 동등하게 잘 동작하는 반응형이 기본 요구다.
5. **장식이 아니라 정직으로 브랜딩한다.** ODO의 인상은 AI 클리셰 이모지가 아니라
   절제된 타이포·정확한 디테일에서 온다.

## Accessibility & Inclusion

한국어 사용자 기준. 별도로 확정된 접근성 표준은 없으나, 반응형(모바일·데스크톱
동등)과 비동기 상태의 명확한 전달이 실질적 접근성 요건이다.
