# 실행할 때마다 쌓이는 생성물. 삭제/덮어쓰기 로직이 없어 계속 커진다
# (model-research/CLAUDE.md "알려진 문제 4"와 같은 문제가 서비스 쪽에도 있다).
# 2026-09-07 기준 output/ 만 274MB였다.
output/
uploads/

# 모델 가중치. `pose_extractor.download_model_if_needed()`가 최초 실행 시
# 자동으로 받는다. 다만 `models/pose_landmarker_full.task`(9MB)는 이미 커밋돼
# 있어 계속 추적된다 — clone 직후 다운로드 없이 바로 돌아가는 편이 낫다는 판단.
# 여기 규칙은 "앞으로 새로 생기는 가중치"에만 적용된다.
models/
*.task
*.pt

venv/
__pycache__/
*.pyc
