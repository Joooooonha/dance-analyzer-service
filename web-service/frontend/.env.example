# 프론트엔드 빌드 설정
#
# 로컬 개발에서는 비워두면 된다 — 기본값이 http://localhost:8080 이다.
# 배포에서는 프론트와 API가 같은 도메인이므로 **빈 문자열**로 두는 것이 맞다.
# 그러면 /api/... 요청이 같은 오리진으로 나가서 쿠키가 자연스럽게 실린다.
#
#   VITE_API_BASE_URL=
#
# API를 다른 도메인에 두는 경우에만 절대 주소를 넣는다:
#   VITE_API_BASE_URL=https://api.내도메인.com
VITE_API_BASE_URL=
