# 작업 목록 — 서비스 이식 및 배포

> **이 파일이 서비스(ai-server / api-server / frontend) 작업의 단일 기준이다.**
> 분석 방법론 연구는 [`model-research/TASKS.md`](model-research/TASKS.md)가 담당하고,
> 이 파일은 **그 결과를 서비스에 반영하는 작업**만 다룬다.
>
> - 작업 시작 전 해당 Phase를 확인한다. 끝내면 체크박스를 갱신한다.
> - **검증하지 않은 것을 완료로 표시하지 않는다.**
> - 범위를 줄이거나 늘릴 때는 이유를 함께 적는다. 조용히 축소·확대하지 않는다.

---

## 현재 구조 (실측 확인)

네 계층이 **이미 분리되어 있다.** 새로 나눌 필요가 없다.

```
[React 19 + Vite]  web-service/frontend/
        │  api/client.js
        ▼
[Spring Boot / Java 17]  web-service/api-server/     (Java 파일 50개)
        │  AiClientService — WebClient로 호출
        │  ai.server.url (기본 http://localhost:8000)
        ▼
[FastAPI / Python 3.10]  ai-server/
        │  POST /upload, POST /analyze, GET /results
        ▼
   분석 엔진 (현재 ai-server 자체 구현, 이식 대상)
```

| 계층 | 역할 | 상태 |
|---|---|---|
| frontend | 업로드·결과 표시 (Dashboard/Practice/Assignments 등) | 페이지 구조 존재 |
| api-server | 인증·과제·연습기록·DB, AI 서버 중계 | Service/Domain/Repository 계층 존재 |
| ai-server | 포즈 추정·정렬·채점·결과물 생성 | **구식 방식** — 이식 대상 |
| model-research | 방법론 연구·검증 | 완료 |

---

## 이식해야 할 내용 (model-research → ai-server)

`ai-server`의 현재 분석 로직과 검증된 방식의 차이. **①②는 명백한 버그**다.

| # | 항목 | 현재 ai-server | 검증된 방식 | 근거 |
|---|---|---|---|---|
| ① | **결측 관절 처리** | `similarity = 1.0` (완벽 일치) | 명시적 마스크, 유효 항목만 평균 | 가려질수록 점수가 오르는 역전 (D2) |
| ② | **회전 메타데이터** | 미처리 | 자동 보정 | iPhone 세로 영상 신뢰도 0.72→0.91 (D20) |
| ③ | 비교 표현 | 좌표 벡터 코사인 | 관절 각도 14종, 몸통 상대 | 실촬영 정렬 0% → 81.6~100% (D1) |
| ④ | 분석 관절 | 22개 (손가락 포함) | 코어 12개 | 손가락은 노이즈만 키움 (D12) |
| ⑤ | 정렬 | fastdtw, 밴드 없음 | `dtw_ndim` + 밴드 + 각속도 | D7, D24 |
| ⑥ | 구간 지정 | 없음 | 시작/끝 입력 + psi 끔 | D23 |
| ⑦ | 점수 | 0~100 가중평균 | 틀린 동작 개수 + 구간 피드백 | 환산 상수가 임의값 (D25) |
| ⑧ | fps 처리 | 단일 | 양쪽 각각 + sanity check | D8 |

> ai-server의 fastdtw는 거리 함수를 넘겨 **다차원으로 올바르게** 호출한다.
> model-research가 겪었던 프레임 경계 버그는 여기 해당되지 않는다.

---

## Phase S1 — 분석 엔진 이식

- [x] `model-research/src/`의 검증된 모듈을 `ai-server/engine/`으로 이식
      (`video_utils` / `sequence` / `features` / `dtw_compare` / `feedback`)
      — import 경로만 `src.` → `engine.`으로 조정, 로직은 그대로
- [x] `engine/adapter.py` — `FramePose` 리스트 → `PoseSequence` 변환
- [x] `engine/analyze.py` — 새 분석 진입점
- [x] `dtaidistance` 설치 (dry-run으로 기존 패키지 무변경 확인 후)
- [x] `main.py`를 새 엔진 호출로 교체 (v3.0). `analyzer.py`는 삭제하지 않고
      "사용 안 함" 표시만 달아 남겨둠
- [x] `/analyze` 요청/응답 스키마 확장 — 구간 지정 파라미터와 구간 피드백
- [x] 기존 소비처 확인 — `result_writer`는 그대로 쓰고, `report_generator` /
      `video_renderer`는 **결함이 있어 `visualize.py`로 교체**했다 (발견 ⑤)
- [x] `requirements.txt`에 `dtaidistance` 추가 — **누락돼 있었다.** 새 환경에서
      설치하면 엔진이 import 단계에서 죽는 상태였다
- [x] 남은 추출 품질 차이 해소 여부 결정 → MediaPipe 유지 (근거는 아래 별도 절)

### S1 진행 중 발견한 것

**① ai-server 환경은 model-research와 다르다 — 그리고 문제없다**

| | model-research | ai-server |
|---|---|---|
| Python | 3.10.18 | **3.13.11** |
| mediapipe | 0.10.21 | **0.10.31** |
| numpy | 1.26.4 | **2.2.6** |
| dtaidistance | 2.3.13 | **2.4.0** (신규 설치) |

CLAUDE.md는 "3.11+에서 mediapipe가 깨진다"고 경고하지만, 확인 결과
**`mp.solutions`만 사라졌고 Tasks API는 정상 동작한다.** ai-server는 Tasks API만
쓰므로 해당되지 않는다. 엔진도 numpy 2.x / dtaidistance 2.4.0에서 정상 동작하며
`psi` 파라미터도 지원된다.

> `ai-server/venv/bin/pip`은 폴더 이동으로 shebang이 깨져 있다.
> `venv/bin/python -m pip`으로 우회하면 정상 동작한다.

**② 검출 실패 프레임이 리스트에서 빠지는 문제 (어댑터에서 수정)**

`extract_from_video`는 포즈를 못 찾은 프레임을 결과 리스트에 **추가하지 않는다.**
그러면 리스트 인덱스와 실제 프레임 번호가 어긋나 **시간축이 왜곡된다**
(100프레임 중 10개 실패 → 90개 리스트가 되어 뒤쪽이 전부 앞당겨짐).
DTW는 시간축 정렬이 핵심이라 치명적이다.
→ 어댑터가 `frame_number`를 인덱스로 삼아 원위치에 되돌리고 빈자리를 채운다.

**③ IMAGE 모드는 VIDEO 모드보다 검출 품질이 낮다**

구간 지정·회전 보정 시 프레임을 직접 순회해야 하는데, 처음에 IMAGE 모드로
구현했더니 품질이 떨어졌다. VIDEO 모드는 프레임 간 추적 정보를 쓰기 때문이다.

| 모드 | natural1 pro 관절 유효율 |
|---|---|
| IMAGE | 88.2% |
| **VIDEO** | **94.8%** |

→ VIDEO 모드로 전환. 타임스탬프가 단조 증가해야 하므로 **잘라낸 구간 안에서
0부터 다시 매기지 않고 원본 프레임 번호 기준**으로 계산한다.
채점률 94.0% → **99.9%** 로 회복.

또한 `extractor.extract_from_frame`은 **호출할 때마다 landmarker를 새로 생성**하므로
쓰지 않는다 (1000프레임이면 모델을 1000번 로드).

**④ 이식 검증 — 같은 백엔드로 맞추면 결과가 일치한다**

| 조건 | 평균 오차 | 고정 오프셋 |
|---|---|---|
| model-research (yolov8n) | 24.41도 | −0.03초 |
| model-research (mediapipe) | 24.03도 | −0.03초 |
| **ai-server (mediapipe)** | **27.64도** | **−0.02초** |

오프셋은 사실상 일치한다. 평균 오차의 잔여 차이(24.03 vs 27.64도)는
**두 구현이 서로 다른 MediaPipe API와 모델을 쓰기 때문**이다.

| | model-research | ai-server |
|---|---|---|
| API | `mp.solutions.pose` (레거시) | Tasks API |
| 모델 | `model_complexity=0` (Lite) | `pose_landmarker_full` (Full) |
| 추출 품질 | 관절 유효율 96.2~97.0% | 94.3~94.8% |

**즉 이식 자체는 정상이고, 남은 차이는 포즈 추출 단계의 모델 차이다.**
정렬·채점·피드백 로직은 동일하게 동작한다.

> **정정 (2026-09-07).** 위 표의 ai-server 수치(27.64도)에는 아래 ⑥의 회전
> 이중 적용 버그가 섞여 있다. 수정 후 다시 재면 평균 28.0도로 큰 변화가 없어
> "남은 차이는 모델 차이"라는 결론 자체는 유지되지만, 위 수치를 그대로
> 인용하지는 말 것.

**⑤ 기존 결과물 생성기 두 개가 DTW 정렬을 쓰지 않고 있었다**

`report_generator.py`와 `video_renderer.py` 모두 **기준 영상 N번 프레임과
연습 영상 N번 프레임을 짝지어** 보여주고 있었다.

```python
# report_generator.generate_report
ref_frame  = self._get_frame(ref_cap,  frame_idx)
prac_frame = self._get_frame(prac_cap, frame_idx)   # ← 같은 인덱스

# video_renderer.create_analysis_video
ret1, ref_frame  = ref_cap.read()
ret2, prac_frame = prac_cap.read()                  # ← 나란히 한 칸씩
```

정렬을 하는 이유 자체가 두 영상의 같은 동작이 **서로 다른 시각**에 있기
때문인데, 정작 사람이 눈으로 보는 결과물에서는 그 정렬이 버려지고 있었다.
natural1은 시작 시점이 3.7초 어긋나 있어 화면의 두 사람이 완전히 다른 동작을
하고 있었다.

추가로 두 모듈은 이미 뽑아둔 포즈를 버리고 다시 추출해서, `/analyze` 한 번에
포즈를 **총 4번** 추출하고 있었다(분석 2회 + 영상 렌더링 2회).

→ `visualize.py`로 교체. DTW 경로로 짝지어진 프레임을 쓰고, 이미 추출한
`PoseSequence`를 재사용한다(`analyze(..., with_context=True)`).
검증: REF 18.90초 ↔ YOU 22.24초가 같은 포즈로 짝지어진 것을 눈으로 확인.

**⑥ 회전 처리가 OpenCV 버전에 따라 달라진다 (이중 회전 버그)**

`visualize.py`가 만든 첫 비교 이미지에서 연습 영상만 **옆으로 누워** 나왔다.
원인은 D20 회전 보정의 전제가 환경에 따라 다르다는 것이었다.

| OpenCV | `CAP_PROP_ORIENTATION_AUTO` 기본값 | `read()`가 주는 프레임 |
|---|---|---|
| 4.11.0 (model-research venv) | 0 | 원본 버퍼 (누운 상태) → **보정 필요** |
| 4.12.0 (ai-server venv) | 1 | 이미 세워진 상태 → **보정하면 안 됨** |

같은 영상(`user_dancer.MOV`, Display Matrix `rotation=-90`)인데 환경에 따라
`apply_rotation`이 필요하기도 하고 해로우기도 하다. ai-server에서는 OpenCV가
이미 돌려준 프레임을 한 번 더 돌려서 누운 프레임이 만들어졌다.

D20의 서술("`cv2.read()`는 매트릭스를 적용하지 않는다")은 **당시 환경에서만
참**이었다. 기본값에 의존하면 배포 환경의 OpenCV 빌드가 바뀔 때 조용히 결과가
달라진다.

→ `open_video()`가 `CAP_PROP_ORIENTATION_AUTO`를 **항상 명시적으로 끄고**
우리가 직접 돌린다. 끄지 못하는 환경에서는 `rotation=0`을 돌려줘 이중 회전을
막는다. 양쪽 venv에서 동일한 결과(rotation 90, 1080x1920 → 1920x1080) 확인.

**이 수정의 효과는 숫자로 주장하지 않는다.** 수정 전후로 지적 구간 수가
47 → 49로 바뀌긴 했지만, 나중에 확인해 보니 **같은 입력을 두 번 돌려도 44~52로
흔들린다**(아래 ⑦). 즉 그 차이는 재현성 잡음과 구분되지 않는다.

확실한 것은 숫자가 아니라 **사용자에게 보여줄 비교 이미지가 사람이 옆으로 누운
채로 나왔다는 것**이고, 그건 수정 후 정상이 됐다. 집계 오차가 크게 안 변한 것은
**각도 feature가 몸통 상대라 카메라 롤에 불변**이기 때문으로 보인다(설계 의도대로).

**⑦ [해결] 같은 입력인데 결과가 매번 달랐다 — 프로세스 격리로 수정**

동일한 영상 쌍·동일한 구간으로 반복 실행했는데 결과가 흔들렸다.

| 실행 | 지적 구간 수 | 평균 오차 | 채점률 |
|---|---|---|---|
| 1회 | 52 | 27.4도 | 83.2% |
| 2회 | 49 | 28.1도 | 83.0% |
| 그 외 관측 | 44, 45, 46, 47 | | |

**상위 10개 순위도 흔들렸다** — 두 실행의 1~10위가 하나도 같은 자리에 오지
않았고, (부위, 시간대) 집합 기준으로 6/10만 겹쳤다.

**원인 추적.** 새 프로세스에서 3회씩 반복해 해시를 비교했다.

| 처리 순서 | 기준 영상 | 연습 영상 |
|---|---|---|
| 기준 → 연습 (1회) | `626ef14a` | `c4499a27` |
| 기준 → 연습 (2회) | `626ef14a` | `9244b72c` **다름** |
| 기준 → 연습 (3회) | `626ef14a` | `cdd4b630` **다름** |
| **연습만** (2회) | — | `9949adf0` **동일** |
| **기준만** (3회) | `626ef14a` **동일** | — |

즉 **영상을 하나만 처리하면 완전히 결정적인데, 해상도가 다른 영상을 앞에서
처리하면 뒤 영상이 비결정적이 된다**(기준 1920x1080 가로, 연습 1080x1920 세로).
새 프로세스에서도 매번 달랐으므로 상태 누적이 아니라 실행 중 비결정성이다.

차이는 반올림 수준이 아니었다: 868프레임 **전부**가 달라지고 최대 121.9픽셀
(프레임 높이의 6.4%), 유효 관절 수도 26028~26058로 흔들렸다. VIDEO 모드가 앞
프레임 결과를 다음 프레임 추적에 쓰기 때문에 작은 차이가 증폭되는 것으로 보인다.

효과 없었던 시도: `OMP_NUM_THREADS` 등 스레드 수 제한 환경변수
(MediaPipe는 자체 스레드풀을 쓴다).

**해법: 영상마다 별도 프로세스에서 추출한다** (`extract_sequence_isolated`).
프로세스 안에서 영상을 하나만 처리하면 위 표대로 결정적이기 때문이다.

수정 후 3회 반복 (`analyze()` 직접 호출):

| 회차 | 구간 수 | 평균 오차 | 기준 해시 | 연습 해시 |
|---|---|---|---|---|
| 1 | 47 | 27.992 | `626ef14a` | `9949adf0` |
| 2 | 47 | 27.992 | `626ef14a` | `9949adf0` |
| 3 | 47 | 27.992 | `626ef14a` | `9949adf0` |

HTTP API로도 2회 반복해 **상위 10개가 순서까지 완전히 동일**함을 확인했다.
연습 영상 해시가 "연습만 처리했을 때"의 값과 같다는 점도 의도한 대로다.

**부수 효과: 오히려 빨라졌다** (같은 조건, 이미지 생성 포함 기준).

| | 수정 전 | 수정 후 |
|---|---|---|
| 이미지 포함 | 약 110초 | **71초** |
| 이미지 없음 | — | 50초 |

프로세스 생성 비용(1~2초)보다, 서버 프로세스에 MediaPipe 상태가 쌓이지 않는
이득이 컸던 것으로 보인다. 주고받는 배열은 수백 KB 수준이라 무시할 만하다.

> 남은 한계: **왜** 해상도가 바뀌면 비결정적이 되는지는 확인하지 못했다.
> MediaPipe/TFLite 내부 동작이라 여기서는 격리로 우회했다.

## Phase S2 — API 계약 확장

- [x] `/analyze` 구간 파라미터 추가 (**모두 선택**, 시작점만 와도 동작).
      최종 필드명은 `reference_*` / `practice_*`로 통일했다 — 나머지 요청 필드가
      이미 `reference_video_path` / `practice_video_path`라 `pro`/`user`를
      섞으면 같은 대상을 두 이름으로 부르게 된다
- [x] Spring `AiClientService` / DTO에 동일 필드 반영
- [x] 응답 스키마: 점수 → **틀린 동작 개수 + 구간 목록 + 확인 어려움 구간**
- [x] Spring `AnalysisResult`에 구간 피드백 필드 추가.
      `totalScore` 컬럼은 `@Deprecated`로 남겨두고 읽지도 쓰지도 않는다
      (`ddl-auto=update`라 컬럼 추가는 자동, 마이그레이션 도구 불필요)
- [x] 안무 구간 저장 위치 결정 → **`Video` 엔티티**
      (`choreographyStartSec` / `choreographyEndSec`).
      "이 영상의 어디부터 안무인가"는 영상 자체의 성질이고, 기준 영상 하나를
      여러 사용자가 따라하면 한 번만 표시하면 된다
- [x] `PATCH /practice-logs/{logId}/trim` 추가 — 프론트 트림 UI가 쓸 저장 API
- [x] **분석을 비동기로 전환** — `POST .../analyze`가 202를 즉시 반환하고
      클라이언트가 `GET /practice-logs/{logId}`를 폴링한다. 실측 202 반환까지
      0.03초, 분석 완료까지 약 1분 45초
- [x] `WebClient` 타임아웃 명시 (응답 10분) — 기본 설정으로는 2분 넘는 분석이
      끊길 수 있다
- [x] 목록 API에서 `score` 제거 → `issueCount`
- [x] Spring ↔ Python JSON 계약 테스트 추가 (`AiAnalysisContractTest`, 7개)

### S2에서 확정된 Spring API (S3/S4 프론트가 이걸 보고 구현)

| 메서드 | 경로 | 용도 |
|---|---|---|
| POST | `/videos` | 영상 업로드. **업로드만으로 분석이 시작되지 않는다** |
| PATCH | `/practice-logs/{logId}/trim` | 안무 구간 저장 (네 값 모두 선택) |
| POST | `/practice-logs/{logId}/analyze` | 분석 시작. **202 즉시 반환** |
| GET | `/practice-logs/{logId}` | **폴링 대상.** `status`가 COMPLETED가 되면 결과가 채워짐 |
| GET | `/practice-logs` | 목록. `issueCount`(분석 전 null) + `status` |

흐름은 **업로드 → 구간 지정 → 분석 요청 → 폴링**이다.
업로드 즉시 자동 분석하던 동작은 제거했다 — 사용자가 구간을 지정하기 전이라
버릴 결과를 2~3분 걸려 만드는 꼴이었기 때문이다.

`GET /practice-logs/{logId}` 응답의 결과 필드:
`issueCount`, `topIssuesJson`, `unreliableSpansJson`, `unreliableNote`,
`qualityJson`, `qualityWarning`, `comparisonVideoUrl`, `jsonResultUrl`,
그리고 양쪽 영상의 `*StartSec` / `*EndSec`.

> 구간 목록은 **JSON 문자열**로 저장한다. 조회 조건으로 쓸 일이 없고 구조가
> 바뀔 수 있어 정규화 이득이 없다. 프론트에서 `JSON.parse` 필요.

**실측 E2E** (natural1, Spring 8081 + AI 8000):
업로드 → trim → analyze(202, 0.03초) → 폴링 약 1분 45초 → COMPLETED,
지적 구간 45개. 상세/목록 API까지 정상 확인.

### S1에서 확정된 `/analyze` 계약 (S2가 이걸 보고 구현)

**요청**

| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `reference_video_path` | string | O | 업로드된 파일명 또는 서버 내 절대경로 |
| `practice_video_path` | string | O | 〃 |
| `reference_start_sec` | float | | 기준 영상 안무 시작 시각 |
| `reference_end_sec` | float | | 기준 영상 안무 끝 시각 |
| `practice_start_sec` | float | | 연습 영상 안무 시작 시각 |
| `practice_end_sec` | float | | 연습 영상 안무 끝 시각 |
| `top_issues` | int | | 기본 10 |
| `generate_images` | bool | | 기본 true — 구간별 비교 이미지 |
| `generate_video` | bool | | 기본 **false** — 정렬 비교 영상(느림) |

> **양쪽 시작점이 모두 있어야** 좁은 밴드가 적용된다(`trim.applied=true`).
> 한쪽만 주면 전역 오프셋이 남아 이득이 없으므로 넓은 밴드 + psi로 처리한다.
> `end_sec <= start_sec`이면 422로 거절한다.

**응답** (`overall_score` 없음)

| 필드 | 설명 |
|---|---|
| `issue_count` | 틀린 동작 구간 **총 개수**. 점수가 아니다 |
| `top_issues[]` | `rank` / `group`(왼팔·오른다리·상체) / `message` / `start_sec` / `end_sec` / `duration_sec` / `severity` / `mean_error_deg` / `parts[]` / `representative` / `comparison_image_url` |
| `top_issues[].representative` | 그 구간에서 오차가 가장 큰 프레임. `practice_sec` / `reference_sec`로 **양쪽 영상 점프**가 가능하다 |
| `unreliable_spans[]` | 평가하지 않은 구간. `unreliable_note`에 이유 문구 |
| `quality` | 채점률·오차 통계·관절 검출률·`warning` |
| `trim` | 요청한 구간이 실제 적용됐는지(`applied`)와 안내 문구 |
| `comparison_video_url` | `generate_video=true`일 때만 |

> `quality`에 **정렬이 맞았는지를 알려주는 값은 없다.** 그걸 추정하려고 신뢰도
> 신호 5종을 시험했지만 전부 정렬 오차를 예측하지 못했다(상관 0.03~0.26).
> `quality.warning`은 **관절 검출 실패율**에만 반응한다 — 이건 직접 측정되는
> 값이다. 임계값 30%는 **잠정값**으로, 사용자 체감과의 관계는 미측정이다.

**실측 소요 시간** (natural1, 36초·29초 영상 / M-시리즈 맥 CPU)

| 옵션 | 소요 |
|---|---|
| 이미지만 (`generate_video=false`) | **71초** |
| 이미지도 영상도 없음 | 50초 |
| 이미지 + 정렬 비교 영상 | 약 100초 (추정) |

(프로세스 격리 수정 후 측정값. 수정 전에는 이미지 포함 약 110초였다.)

→ Spring에서 동기 호출하면 타임아웃이 난다. **비동기 처리 전제로 설계할 것.**

## Phase S3 — 프론트엔드: 시작 시점 선택 UI

**이 UI가 없으면 구간 지정의 이득이 사라진다.** 실측상 입력 오차 ±0.5초면
개선분이 소멸하고, 밴드보다 오차가 크면 성능이 0%로 붕괴한다(절벽형).
따라서 **부가 기능이 아니라 전제 조건**이다.

- [x] **영상 구간 지정 UI** (`components/VideoTrimmer.jsx`) — 타임스탬프만
      수집하고 재인코딩하지 않는다
- [x] 프레임 단위 미세 조정 (±1프레임 = 0.0333초, ±1초 버튼). 실측으로 정확히
      0.0333초씩 움직이는 것 확인
- [x] 끝점은 선택 — "(선택)" 표기 + 해제 버튼. 미지정도 분석 가능
- [x] 지정 결과를 `PATCH /practice-logs/{id}/trim`으로 저장 후 분석 요청
- [x] 흐름 변경: 업로드 → **구간 지정** → 분석 요청 → 결과 페이지 폴링
- [x] 양쪽 시작점이 모두 지정되지 않으면 경고 문구 + 버튼 문구를
      "구간 없이 분석 시작하기"로 바꿔 사용자가 인지하게 함

### S3에서 고친 것

**① CORS에 PATCH가 빠져 있었다**

`CorsConfig`가 GET/POST/PUT/DELETE/OPTIONS만 허용하고 있어, 새로 만든
`PATCH /practice-logs/{id}/trim`이 **프리플라이트에서 막혔다**
(`Response to preflight request doesn't pass access control check`).
메서드를 하나씩 나열하면 새 메서드를 쓸 때마다 같은 문제가 반복되므로
`addAllowedMethod("*")`로 바꿨다 — Origin으로 이미 제한하고 있다.

**② 구간 지정 버튼이 잘못된 시각을 저장했다 (정밀도 버그)**

"현재 지점을 시작으로" 버튼이 React state의 `currentTime`을 읽고 있었는데,
이 값은 `timeupdate` 이벤트로만 갱신된다. seek 직후에는 아직 이벤트가 오지
않아 **이전 값(심하면 0.00초)이 저장됐다.** 브라우저에서 4.07초로 이동한 뒤
바로 누르면 `시작 0.00s`가 찍히는 것을 확인했다.

시작 시각 오차가 ±0.5초를 넘으면 정렬 개선 효과가 사라지므로 그냥 넘길
수준이 아니다. → video 엘리먼트의 `currentTime`을 직접 읽도록 수정.
수정 후 4.07s / 7.77s가 정확히 저장되는 것 확인.

**③ 짝 없는 연습 기록에 분석 버튼이 노출됐다**

업로드 한 번마다 `PracticeLog`가 생기는 구조라, 기준 영상만 올린 경우에도
**기준 영상이 없는 기록**이 하나 남는다. 여기에 분석 버튼을 보여주면 눌러도
반드시 실패한다(`기준 영상이 없어 분석할 수 없습니다`).
→ `referenceVideoId`가 없으면 구간 지정/분석 UI 대신 안내 문구를 보여준다.

> 근본 원인은 아래 S3-후속에서 API 구조를 바꿔 해결했다.

### S3 후속 — 업로드 API 구조 변경

**① 기준 영상 업로드가 더 이상 연습 기록을 만들지 않는다**

기준 영상은 "따라 할 대상"이지 누군가의 연습이 아니다. 그런데 업로드 종류를
가리지 않고 `PracticeLog`를 만들고 있어서, 기준 영상을 올릴 때마다 **비교 대상이
없어 분석이 불가능한 기록**이 하나씩 쌓였다(목록에 영원히 "대기 중").

→ `VideoService.uploadVideo`가 `VideoType.PRACTICE`일 때만 기록을 만든다.

실측: 기준 영상 업로드 후 연습 기록 수 **0개**(이전에는 1개).

**② 업로드 응답에 `logId`를 명시적으로 담는다**

기존 응답은 `videoId`(Long) 하나뿐이었고 프론트가 그 값을 **연습 기록 id로도
그대로 썼다.** 업로드 한 번이 두 테이블에 한 행씩 만들던 동안에는 두 IDENTITY
값이 우연히 같이 늘어나 동작했지만, 보장된 계약이 아니었다.

→ 응답을 `{ videoId, logId, analyzable }`로 바꿨다.

**①을 고치는 순간 이 가정은 실제로 깨졌다.** 같은 흐름을 다시 돌린 결과:

| | 값 |
|---|---|
| `logId` | **2** |
| `practiceVideoId` | **4** |

예전 코드였다면 `/logs/4`로 이동해 존재하지 않는 기록을 열었을 것이다.
즉 두 수정은 함께 가야 하는 한 쌍이었다.

**③ 탭이 백그라운드일 때 결과 갱신이 늦는 문제**

분석이 1~2분 걸려 그 사이 다른 탭을 보는 것이 자연스러운데, 브라우저가
백그라운드 탭의 `setInterval`을 1분에 한 번 수준으로 늦춘다. 실제로 백엔드가
COMPLETED인데 화면은 PROCESSING으로 남아 있는 것을 확인했다(멈춘 것처럼 보임).
→ `visibilitychange`로 **탭이 다시 보이는 순간 즉시 한 번 갱신**한다.

**실측 E2E (브라우저)**: 업로드 → 구간 지정(4.07s/7.77s) → 분석 요청 →
폴링 자동 갱신 → **47개 구간** 표시. 구간 클릭 시 기준 영상 11.93초 /
연습 영상 15.64초로 각각 이동(차이 3.71초 = 지정한 시작점 차이와 일치).
반복 실행해도 47개로 동일 — 재현성 수정이 전체 스택에서 유지됨을 확인.

## Phase S4 — 결과 화면

- [x] 구간 피드백 표시 (상위 10개, 시간대·부위·방향 문구·심각도)
- [x] 개수를 그대로 표시 — 재현성 문제는 발견 ⑦에서 해결됐다
      (같은 입력이면 순위까지 동일)
- [x] "확인 어려움" 구간을 별도 섹션으로 표시 + 제외 이유 문구
- [x] 해당 구간으로 영상 점프 (기준/연습 각각, DTW로 짝지어진 시각)
- [x] 점수 UI 제거 — 상세·목록·숙제 제출 이력 모두 구간 개수로 대체
- [x] 분석 품질 섹션 (채점률·관절 검출률) + 재촬영 경고 문구
- [x] PROCESSING 중 5초 간격 폴링 → 완료 시 자동 갱신

## Phase S5 — 외부 공개 배포

> **콘솔에서 직접 해야 하는 작업은 `DEPLOY-CONSOLE-TASKS.md`에 따로 정리했다.**
> (도메인 구매, 카카오/네이버 앱 등록, R2 버킷, Tailscale 설치 등)

### 확정된 배포 구조

```
브라우저 ──HTTPS──> Cloudflare ──> EC2
                        │          ├─ Spring (API·인증·권한)
                        │          └─ PostgreSQL
                        │
                        ├──> R2 (영상 원본 + 결과물)
                        │      ↑ presigned URL로 직접 업/다운로드
                   Tailscale
                        │
                        └──> 맥미니: FastAPI (분석 전용, 공개 안 함)
```

영상이 **EC2를 지나가지 않는 것**이 핵심이다. 브라우저가 R2로 직접 올리고,
맥미니가 R2에서 직접 받는다. R2는 egress 요금이 0이라 이 트래픽이 전부 무료다.

| 선택 | 이유 |
|---|---|
| **R2** (S3 아님) | egress 무료. 맥미니 다운로드 + 사용자 조회가 이 프로젝트 최대 트래픽 |
| **PostgreSQL** | 데이터가 명확히 관계형(5개 엔티티 join). 분석 결과 JSON은 `jsonb`로 질의 가능하게 |
| **Tailscale** | 맥미니에 공인 IP를 주지 않아도 EC2가 접근 가능 |
| **httpOnly 쿠키 JWT** | localStorage에 토큰을 두면 XSS 한 번에 탈취됨 |
| 단일 도메인 + 경로 분리 | 같은 오리진이라 쿠키 인증이 단순해짐 |

**MongoDB를 쓰지 않는 이유**: `PracticeLog` 하나를 읽는 데 User·Video 2개·
Assignment·AnalysisResult를 전부 join한다. 접근 패턴이 관계형이고 이미 JPA에
얹혀 있어서, 얻는 것(수평 확장)은 이 트래픽에서 0인데 비용은 크다.
"형식이 안 정해진 부분"은 분석 결과 하나뿐이고 그건 `jsonb` 컬럼으로 해결된다.

### S5-1. 인증 — 이것부터 (나머지가 여기 의존)

현재 인증은 `X-User-Id` **헤더 하나**다. 헤더 값만 바꾸면 남의 계정이 되고,
영상 권한 검사도 소유권 검사도 전부 무의미해진다. 외부 공개 전 최우선.

- [x] Spring Security + JWT 도입, `X-User-Id` 제거
- [x] OAuth2 소셜 로그인 (카카오·네이버)
- [x] JWT를 **httpOnly 쿠키**로 전달
- [x] 로컬 로그인은 `dev` 프로파일 전용으로 격리 (운영 비활성)
- [x] `User`에 `provider` / `providerId` 추가
- [x] 비밀번호 BCrypt 해싱 (개발 계정 포함)
- [x] 프론트엔드를 쿠키 인증으로 전환 (`credentials: 'include'`)
- [ ] 리프레시 토큰 — 지금은 7일 만료 후 재로그인. 도입하면 유효기간을 줄일 것

**막았던 구멍**

| 무엇 | 어떻게 뚫렸나 | 지금 |
|---|---|---|
| `X-User-Id` 헤더 | 헤더 숫자만 바꾸면 남의 기록·영상 | 401 |
| `GET /videos/1?userId=1` | URL 숫자만 바꾸면 남의 영상 재생 | 401 |
| 평문 비밀번호 | 저장도 비교도 평문 | BCrypt, 운영엔 저장 자체가 없음 |
| 로그인 실패 메시지 | "존재하지 않는 아이디" / "비밀번호 불일치"를 구분해 계정 존재를 노출 | 동일 메시지 |

> `?userId=` 우회로는 `<video src>`가 헤더를 못 붙인다는 제약 때문에 있었다.
> 쿠키로 옮기니 브라우저가 알아서 실어 보내므로 우회로 자체가 불필요해졌다.

**검증**: 인증 테스트 8개(전체 16개) 통과. 실제 서버 기동 후 두 공격 경로가
401을 반환하는 것과, 로그인 시 `HttpOnly; SameSite=Lax` 쿠키가 발급되고
소셜 로그인 진입점이 카카오/네이버로 302 리다이렉트하는 것까지 확인했다.

> **세션 방식이 아니라 JWT를 쓴 이유**: 분석 요청이 EC2(Spring) → 맥미니로
> 넘어가고 나중에 서버가 늘 수 있는데, 세션은 공유 저장소가 필요하다.
> 다만 JWT는 서버가 무효화할 수 없어서 로그아웃은 쿠키 삭제로만 처리된다.
> 탈취 대응이 필요해지면 블랙리스트나 짧은 만료 + 리프레시를 도입해야 한다.

### S5-2. 저장소 추상화 (완료)

- [x] `Video.videoPath`(로컬 절대경로) → `objectKey`
- [x] 업로드: 브라우저 → presigned PUT → 저장소 직접 (3단계)
- [x] 조회: Spring이 권한 확인 후 짧은 presigned GET 발급
- [x] AI 서버 입출력을 URL로 (로컬 `output/`·`uploads/` 제거)
- [x] **AI 서버의 `/files/*` 정적 마운트, `/upload`, `/results` 제거**
- [x] AI 서버를 `127.0.0.1`에만 바인딩
- [x] `StorageService` 인터페이스 + R2 구현 + 개발용 로컬 구현
- [ ] R2 자격증명 주입 후 실환경 확인 (콘솔 작업 대기)

**업로드가 3단계가 된 이유**

```
1) POST /videos/upload-url    → 서명된 PUT URL 발급
2) PUT  {uploadUrl}           → 브라우저가 저장소에 직접 업로드
3) POST /videos/{id}/complete → 완료 통보 (여기서 연습 기록 생성)
```

파일 바이트가 Spring을 지나가지 않는다. 예전 방식(`MultipartFile`)은
(1) 100MB 영상이 EC2 대역폭·디스크를 쓰고, (2) 경로가 특정 머신에 묶여
분석이 맥미니로 분리되는 순간 파일을 못 찾고, (3) Cloudflare 프록시의 요청
본문 100MB 제한에 걸린다.

2단계는 서버가 완료 시점을 알 수 없으므로 `uploadCompleted` 플래그로 구분한다.

**맥미니에 저장소 자격증명을 주지 않는다**

분석 서버는 R2 키를 갖지 않는다. Spring이 요청마다 필요한 만큼의 짧은 URL을
발급해 넘긴다:

- 영상 2개 → presigned GET
- 결과 이미지 → presigned PUT **10개를 미리** 발급 (구간이 몇 개 나올지 분석
  전에 알 수 없으므로 상위 개수만큼 만들고 쓴 만큼만 사용)

분석 서버는 자기가 올린 객체의 **키를 모른다.** 그래서 URL이 아니라
`image_slot`(몇 번 슬롯에 올렸는지)을 돌려주고, 키를 아는 Spring이
슬롯 → 키로 되돌린다.

맥미니는 집에 있는 홈서버라 EC2보다 물리적 통제가 약하다. 뚫려도 얻는 것이
곧 만료될 URL 몇 개뿐이다.

**URL이 아니라 키를 저장한다**

서명 URL은 만료되므로 DB에 넣으면 얼마 못 가 열리지 않는다. `AnalysisResult`에는
`comparisonVideoKey`와 구간별 `image_key`만 저장하고, **조회할 때마다 새 URL을
만들어** 응답에 담는다.

> 이걸로 "결과 URL이 `127.0.0.1:8000`으로 저장되던" 문제도 함께 사라졌다.
> `path_to_url()`이 요청 호스트를 그대로 URL에 박아 DB에 저장했기 때문에
> 외부 브라우저에서는 절대 로드될 수 없었다.

**개발용 로컬 저장소**

R2 자격증명 없이도 전 과정을 돌릴 수 있도록 `LocalStorageService`를 뒀다
(`storage.type=local`, 기본값). R2와 같은 계약(presigned URL)을 흉내내며
**URL을 HMAC으로 서명**한다 — 개발용이라도 키 문자열만 알면 아무 파일이나
읽고 쓸 수 있게 두면 실수로 배포됐을 때 그대로 구멍이 된다.

**S5-2에서 마주친 것**

| 문제 | 원인 | 해결 |
|---|---|---|
| 업로드 PUT이 400 | 키에 슬래시가 있어 `%2F`로 인코딩했는데 Tomcat이 기본 설정에서 거부 | 키를 경로가 아니라 쿼리 파라미터로 |
| Spring 기동 실패 | `AsyncAnalysisService` ↔ `VideoService` 순환 참조 | 필요한 건 다운로드 URL 하나뿐이라 저장소를 직접 호출 |
| 결과 이미지가 화면에 안 나옴 | 프론트가 삭제된 `comparison_image_url`을 계속 참조 | 응답의 `issueImageUrls` 배열을 쓰도록 교체 + 인라인 표시 |
| `s3-presigner` 의존성 해석 실패 | 그런 아티팩트는 없다 (presigner는 `s3` 안에 있음) | 제거 |

**함께 없앤 정보 노출**: `AssignmentDetailResponse.targetVideoPath`와
`PracticeLogDetailResponse`의 영상 경로 필드가 **서버 내부 저장 경로를 그대로
클라이언트에 내려주고 있었다.** 프론트에서 쓰지도 않았다 — 제거했다.

**실측 검증** (로컬 저장소 기준)

| 항목 | 결과 |
|---|---|
| 업로드 (49MB 2개) | 저장소에 직접 저장, Spring 미경유 |
| 분석 | 지적 구간 47개, 이미지 10개 업로드 성공 |
| 서명 없는 접근 | **403** |
| 다운로드 서명으로 업로드 시도 | **403** (method가 서명에 포함) |
| `../` 경로 탈출 | **차단** |
| 인증 없는 영상 재생 | **401** |
| 결과 이미지 로드 | 10/10, 1689×720 |

> 브라우저에서 결과 페이지를 눈으로 스크롤 확인하는 것은 창이 숨겨진 상태라
> 하지 못했다. 이미지 로드 자체는 `naturalWidth`와 직접 fetch(200/image/jpeg)로
> 확인했다.

### 배포 대상 확정

| 항목 | 결정 |
|---|---|
| EC2 | **새 인스턴스** `t4g.small` (ARM, ap-northeast-2) + Elastic IP |
| 기존 `finder-server`와 합치지 않음 | 2GB에 Spring+Postgres가 안 들어가고, 재배포할 때마다 기존 앱이 위험해진다 |
| 도메인 | `odostudio.site` (가비아 구매 → Cloudflare DNS) |

`t4g.small`로 충분한 근거: 무거운 작업을 전부 옮겼다. 포즈 추출은 맥미니,
영상 파일은 R2 직접 — EC2는 JSON 처리와 권한 판단만 한다.

### S5-3. DB → EC2 PostgreSQL

- [ ] H2 인메모리 탈출 (지금은 **재시작하면 회원·기록이 전부 사라진다**)
- [ ] 분석 결과 JSON 컬럼을 `jsonb`로
- [ ] 프로세스 1개 + 데이터베이스 분리(`dance_db`)

### S5-4. 네트워크 (설정 파일 준비 완료, 적용 대기)

- [x] Nginx 설정 작성 (`deploy/nginx/odostudio.site.conf`)
- [x] systemd unit (`deploy/systemd/dance-api.service`)
- [x] launchd plist (`deploy/launchd/site.odostudio.ai-server.plist`)
- [x] 빌드 스크립트 (`deploy/build.sh`) — 실제 실행 확인 (jar 74M, dist 304K)
- [x] 실행 순서 문서 (`deploy/README.md`)
- [x] **PostgreSQL 드라이버 추가** — 빠져 있어서 prod 기동이 실패했을 상태였다
- [ ] EC2/맥미니에 실제 적용 (콘솔 작업 완료 후)
- [ ] 맥미니 다운 시 재시도 정책
- [x] **AI 서버 동시 실행 제한 + 이벤트 루프 블로킹 수정** (아래)

**`async def`인데 본문이 전부 블로킹이었다**

`/analyze`가 `async def`인데 안에서 하는 일(영상 다운로드, OpenCV, MediaPipe,
서브프로세스)은 전부 블로킹이다. 그래서 분석이 도는 1분 남짓 동안
**이벤트 루프가 통째로 멈춰서 헬스체크조차 응답하지 못했다** — 밖에서 보면
서버가 죽은 것과 구분되지 않는다.

→ 평범한 `def`로 바꿔 FastAPI가 스레드풀에서 실행하게 했다.

**동시 실행 제한**

Spring 쪽에 이미 제한이 있지만 여기서 또 막는다. 같은 Tailscale 망의 다른
경로로 요청이 들어오면 Spring의 제한은 아무 소용이 없기 때문이다.
자리가 없으면 기다리지 않고 **즉시 429 + `Retry-After`**로 돌려보낸다 —
대기시키면 호출자가 응답 없이 묶여 어디서 막혔는지 알 수 없게 된다.

실측 (분석이 도는 중에 측정):

| 확인 | 결과 |
|---|---|
| 헬스체크 응답 | **200, 1~4ms** (수정 전에는 응답 불가) |
| 두 번째 분석 요청 | **429** |
| 첫 번째 분석 | 정상 완료 (200) |

**도메인: `odostudio.site`** (가비아 구매 → Cloudflare DNS)

**Nginx 라우팅에서 주의한 것**

Spring 엔드포인트가 **전부 `/api` 아래에 있지 않다.** OAuth 콜백
(`/login/oauth2/code/...`)은 Spring Security 기본 규칙이고, 경로를 바꾸면
카카오·네이버 콘솔에 등록한 Redirect URI도 같이 바꿔야 한다. 그래서 옮기지 않고
정규식으로 넘긴다:

```
/(api|oauth2|login|videos|practice-logs|assignments|teams)
```

나머지는 SPA 폴백(`try_files ... /index.html`)으로 프론트가 받는다.

**`server.forward-headers-strategy=native`가 없으면 소셜 로그인이 깨진다**

Spring은 자기가 `http://localhost:8080`에 있다고 생각한다. OAuth 설정의
`{baseUrl}`이 그대로 치환되어 카카오에 `http://localhost:8080/login/...`을
redirect_uri로 보내고, 콘솔 등록값과 달라 거부된다. 통과하더라도 사용자를
localhost로 돌려보낸다. Nginx가 넘기는 `X-Forwarded-Proto`를 신뢰하게 해야 한다.

**Cloudflare 실제 IP 복원**: `set_real_ip_from` + `real_ip_header CF-Connecting-IP`가
없으면 모든 요청 출발지가 Cloudflare IP로 찍혀 접근 로그가 쓸모없어진다.

**Full (strict) 적용 순서**: Origin Certificate를 EC2에 **먼저** 넣고 그 다음
모드를 바꾼다. 순서를 뒤집으면 그 사이 사이트가 526으로 죽는다.

### S5-5. 마무리 (일부 완료)

- [x] 하드코딩 제거: 업로드 경로(저장소 전환으로 소멸), 프론트 `API_BASE_URL`
      (`VITE_API_BASE_URL`), 나머지는 환경변수화
- [x] **분석 동시 실행 제한** (아래 참조)
- [x] 업로드 파일 검증 — Content-Type 화이트리스트(mp4/mov/webm).
      예전에는 `isEmpty()` 하나뿐이라 100MB짜리 아무 파일이나 받았다
- [x] **운영 프로파일에서 개발용 로그인이 실제로 사라지는지 검증** (아래 참조)
- [ ] DB URL → 환경변수 (S5-3에서 함께)

**[중요] `@Profile`을 컨트롤러 메서드에 붙이는 것은 아무 효과가 없다**

S5-1에서 "로컬 로그인은 `dev` 프로파일 전용"이라고 적어두고 **확인하지 않았는데,
테스트를 써 보니 틀렸다.** 운영 프로파일에서:

| 엔드포인트 | 기대 | 실제 |
|---|---|---|
| `POST /api/auth/signup` | 404 | **200 — 계정이 만들어졌다** |
| `POST /api/auth/login` | 404 | 400 (경로는 살아 있음) |

`@Profile`은 **빈(클래스/`@Bean` 메서드) 단위로만** 동작하고 요청 매핑 메서드에는
적용되지 않는다. 즉 운영에 비밀번호 계정 생성 경로가 그대로 열려 있었고,
"평문 비밀번호 문제가 사라졌다"는 앞선 정리도 그만큼 틀린 말이었다.

→ `DevAuthController`를 **별도 클래스**로 분리했다. 프로파일로 무언가를 끄려면
반드시 클래스를 나눠야 한다. `ProdProfileTest`로 404를 고정했다.

> 교훈: "운영에서는 비활성"은 **말이 아니라 테스트로** 확인해야 한다.

**분석 동시 실행 제한**

`@Async` 기본 실행기(`SimpleAsyncTaskExecutor`)는 요청마다 스레드를 무제한
생성한다. 분석 한 건이 CPU를 1~2분 점유하므로, 동시 요청 몇 개면 서로 코어를
나눠 갖다가 전부 느려지고 아무것도 제때 끝나지 않는다.

→ `analysisExecutor`: **동시 1건 + 대기열 20**. 넘치면 조용히 버리지 않고
거절한다 — "분석 중"을 보여주다 영영 끝나지 않는 것이 가장 나쁘다.
종료 시에는 진행 중인 분석을 기다린다(끊기면 PROCESSING에 영원히 멈춘다).

병렬로 돌려 다 같이 느려지는 것보다, 순서대로 처리해 앞 사람이 정상 속도로
끝나는 편이 낫다는 판단이다.

**시드 계정 테스트에서 배운 것**: 처음에 `userRepository.count() == 0`으로 확인하려
했는데 실패했다. 원인은 코드가 아니라 테스트 환경이었다 — `jdbc:h2:mem:testdb`는
**이름이 같으면 같은 JVM 안에서 DB를 공유**해서, dev 프로파일 테스트가 만든 시드
계정이 prod 컨텍스트에도 보였다. 그 검사로는 "InitDb가 안 돌았다"와 "다른 테스트
데이터가 섞였다"를 구분할 수 없어, **빈 존재 여부**를 보는 쪽으로 바꿨다.
- [ ] 프레임 서브샘플링 적용 여부 (정렬만 서브샘플, 채점은 전체 프레임)
- [ ] 맥미니 launchd 서비스 등록, Python 3.10 고정 확인

---

## 확정된 결정

| 항목 | 결정 | 근거 |
|---|---|---|
| **포즈 모델** | **현 상태 유지** (MediaPipe `pose_landmarker_full`) | 아래 근거 참조 |
| **점수** | **제거. DB `overall_score` 컬럼은 남겨두고 쓰지 않음** | 환산 상수가 임의값이라 절대값에 의미 없음. 컬럼을 그대로 두면 **스키마 변경이 없어 Flyway 등 마이그레이션 도구가 불필요** |
| **구간 지정** | **시작·끝 모두 선택 입력.** 시작점만 있어도 동작 | 실측: 시작만으로도 전역 오프셋이 제거됨. 끝점 추가 이득은 4.7%p로 작음(아래) |
| **입력 UI** | **영상 앞뒤 자르기(트림) 형태** | "몇 초인지 입력"보다 익숙한 패턴이고, 시작·끝을 자연스럽게 함께 얻는다. **단 실제로 영상을 자르지 않고 타임스탬프만 수집** — 재인코딩 시 화질 손실·시간 소요가 있고 원본이 결과 재생에 필요 |
| **품질 게이트** | **보류** | 임계값을 정할 실패 사례가 없음. 측정값 노출만 우선 가능 |

### 포즈 모델 결정 — MediaPipe → YOLO (2026-09-09 변경)

**결론: YOLO-pose(`yolov8n-pose`)를 쓴다.**

품질 순위는 처음부터 YOLO가 위였다 (뼈 안정성, 낮을수록 좋음).

| 모델 | 뼈 불안정 ↓ | fps |
|---|---|---|
| **yolo11x-pose** | **0.0127** | 5.7 |
| ViTPose-base | 0.0136 | 6.0 |
| **yolov8n-pose** | **0.0143** | 26.0 |
| RTMPose-l | 0.0150 | 3.6 |
| MediaPipe Full (구 ai-server) | 0.0189 | 29.1 |

그런데도 MediaPipe를 유지했던 유일한 이유는 **교체 비용**이었다(패키지 37개 +
numpy 업그레이드). 배포 환경이 정해지면서 그 판단이 뒤집혔다.

**결정적 이유 1 — 설치가 안 된다**

분석 서버가 Fedora Asahi Remix(Apple Silicon 위 Linux, **aarch64**)로 정해졌다.
PyPI 휠 확인 결과:

| MediaPipe | linux aarch64 휠 |
|---|---|
| 0.10.21 (연구용 핀) | ❌ 없음 |
| 0.10.31 (당시 ai-server) | ❌ **없음** (리눅스는 x86_64만) |
| 1.0.1 (최신) | ✅ 있음 |

1.0.1로 올리면 되지만 `0.10 → 1.0` 메이저 업그레이드라 Tasks API 호환성이
불확실하다. 반면 YOLO는 `torch`가 `manylinux_2_28_aarch64`(cp310~cp313),
`ultralytics`가 `py3-none-any`로 문제가 없다.

**결정적 이유 2 — 검증 수치가 서비스에 적용되지 않고 있었다**

정렬 정확도(앵커 기준 ±5프레임 내 71.8% → 81.6%)를 잰 백엔드는 **YOLO**인데
서비스는 MediaPipe로 돌고 있었다. 즉 **문서의 검증값이 서비스의 값이라고 말할 수
없는 상태**였다. 같은 백엔드를 쓰면서 이 간극이 사라졌다.

**실측 (natural1, 동일 구간 지정)**

| | MediaPipe Full | **YOLO (yolov8n)** |
|---|---|---|
| 평균 오차 | 28.0도 | **24.9도** |
| 채점률 | 81.7% | **83.6%** |
| 소요 (이미지 없이) | 약 50초 | **35~38초** |
| 관절 유효율 (기준/연습) | 0.940 / 0.910 | 0.938 / 0.909 |

연구에서 잰 yolov8n 값(24.41도)과 24.9도로 근접해, 이식이 정상임을 함께 확인했다.

**재현성 유지 확인**: 2회 반복에서 구간 수·평균 오차·시퀀스 해시·상위 5개가 모두 동일.
프로세스 격리는 그대로 유지한다 — YOLO에서 같은 상태 공유가 없다고 확인한 것이
아니고, 비용이 거의 없어 안전한 쪽을 택했다.

**남은 과제**: `yolo11x-pose`(뼈 안정성 1위, 0.0127)는 4.6배 느려서 보류.
겹침 데이터로 실이득을 확인한 뒤 재검토한다.

**macOS 개발 환경 주의**: `ultralytics`가 macOS에서만 numpy 2.0~2.3.4를 배제하는데
`opencv-python-headless`는 numpy<2.3을 요구해 제약이 겹치지 않는다. numpy 2.2.6으로
실제 동작을 확인했으므로 `pip check` 경고는 무시해도 된다.
**배포 환경(Linux)에는 이 충돌이 없다.**

### model-research ↔ ai-server 동등성 검증 (2026-09-09)

"연구에서 개선한 것이 서비스에 다 반영됐는가"를 **말이 아니라 대조로** 확인했다.

**1) 엔진 파일 — 완전 동일**

| 파일 | 결과 |
|---|---|
| `sequence.py` / `features.py` / `dtw_compare.py` / `feedback.py` / `video_utils.py` | ✅ import 경로 제외 **바이트 동일** |

**2) 분석 파라미터 — 동일**

`window`(밴드 0.5초), `psi`, `max_time_diff=0.6`, `min_separation=0.3`,
`min_valid_ratio=0.3`, `top_issues=10` 모두 일치.

**3) 모델 가중치 — 동일** (`yolov8n-pose.pt`, md5 `fce9c3a495cc` 일치)

**4) 실제 출력 대조**

처음에는 어긋났다. 원인은 **ultralytics 버전 차이**였다(연구 8.3.155 vs 서비스 8.4.144).
버전을 맞추자 대부분 좁혀졌다.

| | 연구 1회 | 연구 2회 | ai-server |
|---|---|---|---|
| 지적 구간 | 51 | **50** | **50** |
| 평균 오차 | 25.34 | 25.50 | **25.49** |
| 중앙 오차 | 22.02 | 22.00 | **21.99** |
| 채점쌍 | 821 | 818 | **819** |

**5) 남은 차이는 잡음이었다 — 연구 코드 자체가 재현되지 않는다**

같은 입력으로 연구 파이프라인을 두 번 돌렸더니 구간 51 → 50, 채점쌍 821 → 818로
달라졌다. **ai-server의 값은 그 변동 범위 안에 있다.**

역전된 사실 하나: **ai-server는 재현되는데(2회 시퀀스 해시·구간 수·상위 10개 모두
일치) 연구 코드는 재현되지 않는다.** 프로세스 격리를 넣은 쪽만 안정적이다.
즉 서비스가 연구보다 **덜 정확한 것이 아니라 더 안정적**이다.

> 검증하지 않고 넘어갔다면 "결과가 다르다 → 이식이 잘못됐다"고 오판했을 것이다.
> 실제로는 기준으로 삼은 쪽이 흔들리고 있었다.

**아직 옮기지 않은 것** (의도적)

`model-research/src`의 나머지 파일은 **연구 전용**이라 서비스에 필요 없다:

| 파일 | 용도 |
|---|---|
| `pose_hybrid` / `pose_vitpose` / `pose_rtmpose` / `pose_factory` | 모델 비교 실험용. 서비스는 YOLO 하나만 쓴다 |
| `evaluate.py` / `metrics.py` | mAP 측정용 |
| `detection.py` | hybrid 파이프라인 1단계 |
| `main.py` | CLI. 서비스는 HTTP API가 대신한다 |

`align_two_pass`(dtw_compare.py)는 **양쪽 모두에 있지만 어디서도 호출하지 않는다.**
2단계 정렬은 시도했다가 자연 촬영에서 개선이 재현되지 않아 채택하지 않았다
(TASKS.md "개선 시도 — 2단계 정렬(미완성)"). 죽은 코드지만 시도 기록으로 남긴다.

### 구간 지정 실측 근거### 구간 지정 실측 근거

| 쌍 | 설정 | ±5프레임 내 | 최대 오차 |
|---|---|---|---|
| natural1 | 시작+끝, psi 끔 | 81.6% | 0.73초 |
| natural1 | **시작만, psi 끔** | **76.9%** | 0.73초 |
| natural1 | 시작만, psi=3초 | 72.2% | **3.23초** |
| natural2 | 시작+끝, psi 끔 | 92.9% | 0.23초 |
| natural2 | **시작만, psi 끔** | **86.7%** | 0.23초 |
| natural2 | 시작만, psi=3초 | 75.0% | **0.87초** |

**⚠️ 예상이 뒤집힌 지점**: 처음에는 "끝점을 안 받으면 뒤쪽이 불확실하니 psi를
켜야 한다"고 판단했으나 **실측은 반대였다.** psi를 켜면 natural1의 최대 오차가
0.73초 → 3.23초로 4배 이상 악화된다. 시작점이 확정되면 좁은 밴드가 대각선을
잡아주는데, psi가 그 위에서 양 끝을 또 건너뛰게 해 경로를 흐트러뜨린다.
**밴드와 psi가 같은 역할을 중복 수행하며 서로 방해하는 것으로 보인다.**

→ **시작점이 지정되면 끝점 유무와 무관하게 psi를 끈다.**

---

## 물려받는 한계 (서비스 설계 시 고려)

model-research에서 확인된 것들. 서비스가 이걸 **숨기지 말고 다뤄야** 한다.

- **잘 추는 사람에게 정확하고 못 추는 사람에게 부정확하다.** 사용자가 틀릴수록
  정렬이 어긋나고 채점도 틀린다. 주 사용자가 후자라는 점에서 구조적 한계다.
  → 완화책: 신뢰도 낮은 구간(90도 초과)을 지적하지 않고 "확인 어려움"으로 분리
- **정렬 오류를 자동 감지할 수 없다.** 후보 신호 5개 모두 예측력 없음
  (실패가 "확신에 찬 틀린 매칭"이라 원리적으로 감지 불가)
- **피드백 내용의 타당성이 미검증이다.** 파이프라인이 의도대로 도는 것까지만 확인됨
- **겹침 구간 성능 미검증.** 보유 영상 8개 모두 검출 실패 0프레임이라 스트레스 안 됨
- **임계값들이 잠정값이다** (신뢰 불가 90도, 구간 병합 0.5초 등)
- 정렬 정확도 현황: 중앙 오차 0.067~0.100초, ±5프레임 내 81.6~100%,
  최대 오차 0.17~0.60초

---

## 관련 문서

| 문서 | 내용 |
|---|---|
| [`model-research/TASKS.md`](model-research/TASKS.md) | 분석 방법론 진행 상태, 설계 결정 D1~D25, 실측 로그 |
| [`docs/analysis-engine-report.md`](docs/analysis-engine-report.md) | 기술 보고서 — 전제·실험 설계·결과 해석 |
| [`README.md`](README.md) | 프로젝트 개요 |
